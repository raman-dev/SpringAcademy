Request Processing Lifecycle
----------------------------
                      ------------                          ------------
       request  ->   | dispatcher | -> dispatch request -> | controller |
       response <-   |  servlet   | <-      data        <-  ------------
                      ------------
                          |     |
                     json |     | data
                      xml |     |
                          |     v
                        -----------
                       |  message  |
                       | converter |
                        -----------
      
      - developers only need to create controllers
      
      - Controller is a POJO(plain old java object) with an @Controller annotation
      - @GetMapping tells spring what method to use to process a get request 
      - use @ResponseBody annotation to return results 


Message Converter
------------------
    - Convert from java object to correct response type
    - HttpMessageConverter convert request/response body data 
      XML,GSon, Json,etc, ...


JAR or WAR Configuration packaging
-----------------------------------
    - spring boot comes with a default embedded web container (Tomcat)
      can run web server from the command line
